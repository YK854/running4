<h4>本の一覧画面</h4>
<p>こちらの画面は投稿された本の一覧が表示される画面になります！</p>
<table>
    <tr>
        <th>ID</th>
        <th>タイトル</th>
        <th>説明</th>
    </tr>
    <% @books.each do |book| %>
        <tr>
            <td><%= link_to book.id, book_path(book.id) %></td>
            <td><%= link_to book.title, book_path(book.id) %></td>
            <td><%= link_to book.body, book_path(book.id) %></td>
            <td><%= link_to '削除',book_path(book.id),method: :delete %></td>
        </tr>
    <% end %>
</table>